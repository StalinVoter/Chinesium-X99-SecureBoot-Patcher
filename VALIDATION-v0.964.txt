Chinesium X99 SecureBoot Patcher v0.964 validation
==================================================

New v0.964 coverage
-------------------
- FPT bracketed cursor addresses are not used as GUI progress identities for any phase;
- a real-hardware erase trace from 1% through 100% maps to one stable erase bar;
- a real-hardware program trace from 0% through 100% maps to one stable program bar;
- read and verify remain one stable bar per pass;
- a genuine second erase/program pass is detected when the percentage resets and receives a new bar;
- the supplied successful XD3 flash transcript replays as exactly four target-flash bars: Reading, Erasing, Programming, Verifying.

Retained v0.963 coverage
------------------------
- determinate Secure Boot creation progress bar;
- target-FV containment of UEFIReplace rebuild output;
- original bytes preserved outside the validated target FV;
- exact SPI-capacity dump validation;
- dual byte-identical pre-flash backups;
- automatic verified-backup recovery after destructive-stage flash failure;
- de-duplicated master session logging and LOGS-folder build reports.

Current container validation
----------------------------
55 tests passed; 19 optional reference-ROM tests were skipped. The compact and classic XD3 reference ROMs were available; the remaining reference corpus was not. Run: X99_SB_TEST_DATA=<reference directory> python3 -m pytest -q -p no:cacheprovider.

PK recognition uses the exact validated decompressed payload SHA-256 and a generic production-PK label. Tests confirm that recognition succeeds for the bundled payload and that a changed FFS still fails the independent whole-file donor hash check.

All authored source and documentation were scanned case-insensitively for the excluded manufacturer name, including filenames and ASCII/UTF-16/UTF-32 byte representations: zero matches. The existing compressed signed PK certificate retains its original issuer/subject data; certificate contents were not renamed.

This revision was tested on Linux with Python 3.12. Windows executables were not rebuilt here; Windows GUI and physical flashing were not retested.

During the earlier v0.964 validation, the supplied successful hardware flash transcript was also replayed through the v0.964 progress sequencer. The target flash produced exactly:
- read:1       0..100%
- erase:1      1..100%
- program:1    0..100%
- verify:1     0..100%
